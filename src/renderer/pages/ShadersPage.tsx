import ContentPage from "./ContentPage";

export default function ShadersPage() {
  return (
    <ContentPage
      category="shader"
      title="Shaders"
      subtitle="Browse and install Modrinth shader packs into your instances."
      emptyTitle="No shaders found"
      emptyDescription="Try another search or filter."
      singleName="Shader"
    />
  );
}
